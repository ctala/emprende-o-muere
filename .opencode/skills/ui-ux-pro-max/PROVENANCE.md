# Provenance

Vendored from https://github.com/nextlevelbuilder/ui-ux-pro-max-skill at
v2.13.0 (MIT — `LICENSE` preserved next to this file). Only the
`ui-ux-pro-max` skill directory was kept (SKILL.md, data/, references/,
scripts/); the upstream CLI/gallery/screenshots/stack assets were dropped to
keep this repository small. The search script paths inside SKILL.md were
repointed from the upstream plugin layout to `.opencode/skills/ui-ux-pro-max/`.
Update by re-cloning upstream and repeating the same steps; never edit
`data/` files by hand.
