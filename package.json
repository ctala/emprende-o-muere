{
  "name": "emprende-o-muere",
  "version": "0.2.0",
  "license": "MIT",
  "private": true,
  "description": "Turn-based roguelite about founding a startup in Latin America. Vanilla JS, no build step.",
  "scripts": {
    "test": "node --test 'tests/*.test.js'",
    "test:e2e": "node --test 'tests/e2e/*.test.js'",
    "gate": "node --test 'tests/*.test.js' && node --test 'tests/e2e/*.test.js'"
  }
}
